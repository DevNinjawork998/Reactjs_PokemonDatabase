import React from "react";
import { Pagination } from "react-bootstrap";

function PaginationComponent(total, current, onChangePage) {

//     let items = []
//     if (current > 1) {
//         items.push(<Pagination.Prev key={0} onClick={() => onChangePage(current - 1)} />)
//     }
//     for (let page = 1; page <= total; page++) {
//         items.push(<Pagination.Item key={page} active={page === current} onClick={() => onChangePage(page)}>{page}</Pagination.Item>)
//     }
//     if (current < total) {
//         items.push(<Pagination.Next key={total + 1} onClick={() => onChangePage(current + 1)} />)
//     }

//     return (
//         <Pagination>{items}</Pagination>
//     )
};

export default PaginationComponent;
